services:
  - type: web
    name: four-wall-web-beta
    runtime: static
    rootDir: web-app
    buildCommand: npm ci && npm run build
    staticPublishPath: dist
    pullRequestPreviewsEnabled: true
    envVars:
      - key: VITE_API_BASE_URL
        sync: false
