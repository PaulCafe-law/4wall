# CLAUDE.md

`AGENTS.md` is the single source of truth for repo policy — read it first.

This file is only a tool-compatibility shim (entrypoint hint) and intentionally
holds no policy of its own. Do not add rules here; put them in `AGENTS.md`.
