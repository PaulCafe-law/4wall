# Provisioned once by the 4WALL backend. Keep this file mode 0600.
FOURWALL_OPENBMC_CONNECTOR_TOKEN=fwobmc_REPLACE_WITH_PROVISIONED_TOKEN

# Prefer loopback when the connector runs on nckusoc beside the collector.
FOURWALL_OPENBMC_COLLECTOR_URL=http://127.0.0.1:8080
