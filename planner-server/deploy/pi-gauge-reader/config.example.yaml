camera:
  source: "rtsp://user:pass@192.168.1.10:554/stream1"
  capture_mode: "ffmpeg_single_frame"
  rtsp_transport: "tcp"
  interval_sec: 15
  capture_timeout_sec: 8
  capture_retries: 3
  min_meter_crop_width_px: 180

status:
  host: "127.0.0.1"
  port: 8091

mqtt:
  enabled: false
  host: "192.168.1.50"
  port: 1883
  base_topic: "4wall/liancheng/injection/m01"
  client_id: "fourwall-gauge-reader-pi5"

platform:
  enabled: true
  api_base_url: "https://four-wall-api.onrender.com"
  device_token: "REPLACE_WITH_CAMERA_DEVICE_TOKEN"
  timeout_sec: 8

gauges:
  - id: "press_am_meter"
    label: "PRESS AM METER"
    unit: "A"
    min_val: 0
    max_val: 10
    calibration_file: "gauges.json"

  - id: "flow_am_meter"
    label: "FLOW AM METER"
    unit: "A"
    min_val: 0
    max_val: 10
    calibration_file: "gauges.json"

debug:
  runtime_dir: "runtime"
  save_annotated: true
  save_failed_frames: true
  rolling_keep: 200
  crops_for_training: false
